# SpreadIQ Environment Configuration
# No API keys needed! Uses free NSE & public APIs

# Server Port (for local development)
PORT=5000

# Node Environment
NODE_ENV=development

# Note: This app uses:
# - NSE API (Free, real-time, no key)
# - Public market data (no authentication needed)
